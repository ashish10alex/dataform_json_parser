name: goreleaser

# on:
#   push:
#     tags:
#       - '*'


# TODO:
# Add GITHUB token
# Change on: when a tag is pushed
# Change deploy.sh to be dynamic for testing locally and real release

on:
  pull_request:

permissions:
  contents: write

jobs:
  goreleaser:
    runs-on: ubuntu-latest
    steps:
      -
        name: Checkout
        uses: actions/checkout@v4
        with:
          fetch-depth: 0
      -
        name: Install GoReleaser
        uses: goreleaser/goreleaser-action@v6
        with:
          install-only: true
      -
        name: Show GoReleaser version
        run: goreleaser -v

      -
        name: Run releaser for testing
        run: chmod +x ./deploy.sh; ./deploy.sh
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

